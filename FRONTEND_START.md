# 🦎 Zig Trainer Frontend — Быстрый старт

Frontend для платформы Zig-упражнений на React + TypeScript.

## 🚀 Быстрый старт (3 минуты)

### 1. Установка

```bash
cd frontend
npm install
```

### 2. Запуск

```bash
npm run dev
```

Фронтенд откроется на http://localhost:5173

### 3. Использование

1. **Выбрать задачу**
   - На главной странице появится список доступных задач
   - Нажмите на карточку задачи

2. **Написать решение**
   - Откроется редактор кода с примером Zig кода
   - Поддержка Tab для отступов
   - Счётчик строк

3. **Отправить на проверку**
   - Нажмите кнопку "✅ Check"
   - Получите `job_id` в ответе

4. **Следить за результатами**
   - Статус обновляется автоматически каждые 1 секунду
   - Когда `state` станет "done" — появятся результаты:
     - ✅ OK/WA/CE/RE/TLE verdict
     - Stdout/stderr
     - Compile log
     - Test results по каждому тесту

---

## 📂 Компоненты

### CodeEditor
- Текстовый редактор для Zig кода
- Поддержка Tab (4 пробела)
- Счётчик строк
- Отображение текущего языка

### TaskList
- Список доступных задач
- Карточки с градиентом и анимацией
- Индикация текущей задачи

### TaskView
- Просмотр условия задачи
- Метаданные (time limit, memory)
- Подсветка синтаксиса Zig в примерах кода

### ResultView
- Статус job (queued/running/done/error)
- Позиция в очереди
- Время выполнения
- Verdict с цветовой индикацией
- Детали компиляции и stderr
- Результаты тестов

---

## 🎨 Фичи

- Автоматический поллинг статуса (каждые 1 сек)
- Плавные анимации переходов
- Адаптивный дизайн
- Цветовая индикация verdicts

---

## ⚙ Конфигурация

URL Backend по умолчанию:
```bash
http://127.0.0.1:8000
```

Изменение URL:
```bash
VITE_API_BASE_URL=http://your-backend-url:8000 npm run dev
```

---

## 🐛 Развертывание

### Сборка

```bash
npm run build
```

Фронтенд будет собран в `frontend/dist/`

### Запуск production

```bash
npx serve dist -l 80
```

---

## 🧪 Тестирование сценариев

### Сценарий 1: Правильное решение
```zig
const std = @import("std");
pub fn main() !void {
    try std.io.getStdOut().writer().print("Hello, World!", .{});
}
```
**Результат:** ✅ OK

---

### Сценарий 2: Неправильный вывод
```zig
const std = @import("std");
pub fn main() !void {
    try std.io.getStdOut().writer().print("Wrong!", .{});
}
```
**Результат:** ❌ WA

---

### Сценарий 3: Ошибка компиляции
```zig
const std = @import("std");
pub fn main() !void {
    this is bad zig syntax
}
```
**Результат:** ⚠️ CE

---

## 📝 Полная документация

Смотрите:
- `frontend/README.md` — полное описание фронтенда
- `../README.md` — документация всего проекта (backend + frontend)

---

## 🔧 Troubleshooting

### Frontend не запускается

Проверьте порт:
```bash
netstat -an | grep 5173
```

Порт занят? Измените в `frontend/vite.config.ts`:
```ts
server: {
  port: 5174,
}
```

### API ошибки

Проверьте что Backend запущен:
```bash
curl http://127.0.0.1:8000/health
```

---

## 🎯 Советы

- Используйте VS Code с расширением Zig для подсветки синтаксиса
- Frontend автоматически поллит статус — не нужно обновлять вручную
- При долгом выполнении (> 10 сек) — возможен timeout на сервере

---

## 📧 Разработка

### Структура файлов

```
frontend/src/
├── components/
│   ├── CodeEditor.tsx
│   ├── TaskList.tsx
│   ├── TaskView.tsx
│   └── ResultView.tsx
├── App.tsx
├── App.css
└── main.tsx
```

### Добавление нового компонента

1. Создать файл `frontend/src/components/NewComponent.tsx`
2. Импортировать в `App.tsx`
3. Добавить роут в `App.tsx`

---

## 🚀 Запуск на VPS

Если деплоите на VPS:

1. **Backend:** systemd service (см. корневой README)
2. **Frontend:** Nginx reverse proxy на порт 5173
3. **Health check:** curl http://localhost:8000/health

---

## 💡 Поддержка

Вопросы или проблемы:
- GitHub Issues: https://github.com/kuzmin-lemmi/MSP---zig/issues

---

**Готово к работе!** ✨

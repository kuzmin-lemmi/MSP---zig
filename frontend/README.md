# Zig Trainer Frontend

Frontend для платформы Zig-упражнений на React + TypeScript.

## Особенности

- 📋 Список задач с фильтрацией по модулю
- 📄 Просмотр условия задачи с подсветкой синтаксиса
- ✏️ Редактор кода с автотабом и поддержкой Zig
- 🔄 Автоматическое поллинг статуса выполнения
- 📊 Детальная визуализация результатов (stdout, stderr, verdict, test results)
- 🎨 Современный UI с анимациями и адаптивным дизайном

## Технологический стек

- **React 18** с TypeScript
- **Vite** как сборщик
- **React Router** для навигации
- **Lucide Icons** для иконок
- **Axios** для API запросов
- **Tailwind CSS** для стилей (будет добавлено)

## Установка

```bash
cd frontend
npm install
```

## Запуск разработки

```bash
cd frontend
npm run dev
```

Frontend будет доступен на http://localhost:5173

## Конфигурация

Для изменения URL API Backend:

```bash
VITE_API_BASE_URL=http://localhost:8000 npm run dev
```

## Структура проекта

```
frontend/
├── src/
│   ├── components/
│   │   ├── CodeEditor.tsx
│   │   ├── TaskList.tsx
│   │   ├── TaskView.tsx
│   │   └── ResultView.tsx
│   ├── App.tsx
│   ├── App.css
│   └── main.tsx
├── index.html
├── package.json
└── README.md
```

## Компоненты

### CodeEditor
Текстовый редактор для Zig кода с подсветкой синтаксиса.

**Функции:**
- Ввод кода с поддержкой Tab
- Счётчик строк
- Отображение текущего языка

### TaskList
Список доступных задач.

**Функции:**
- Отображение карточек задач
- Выбор задачи для решения
- Индикация текущей задачи

### TaskView
Просмотр условия задачи.

**Функции:**
- Отображение заголовка и метаданных
- Подсветка синтаксиса Zig в условии
- Отображение лимитов (time, memory)

### ResultView
Визуализация результатов выполнения.

**Функции:**
- Статус job (queued, running, done, error)
- Позиция в очереди
- Время выполнения
- Verdict с цветовой индикацией
- Детали компиляции
- Stderr
- Результаты тестов
